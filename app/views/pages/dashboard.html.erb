<h1>Mes réservations</h1>
<% if @bookings.length == 0 %>
  <p>'Vous n'avez pas de réservation en cours !'</p>
<% else %>
  <ul>
    <% @bookings.each do |booking| %>
      <li><strong><%= booking.artwork.title %></strong> - du <%= booking.start_date %> au <%= booking.end_date %></li>
    <% end %>
  </ul>
<% end %>
